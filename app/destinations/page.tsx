"use client";
import Link from "next/link";
import {useEffect,useMemo,useState} from "react";
import ManagedHero from "../components/ManagedHero";
import {createClient} from "../../utils/supabase/client";
type Row=Record<string,unknown>;
const fallback=[["Beijing","Imperial history & the Great Wall"],["Xi'an","Ancient China & Terracotta Warriors"],["Zhangjiajie","Otherworldly mountains & nature"],["Chengdu","Pandas, tea houses & Sichuan life"],["Chongqing","Mountain-city energy & karst scenery"],["Guilin","Rivers, limestone peaks & countryside"]];
export default function Destinations(){const supabase=useMemo(()=>createClient(),[]);const [items,setItems]=useState<Row[]>([]);useEffect(()=>{supabase.from("destinations").select("*").eq("is_active",true).order("sort_order",{ascending:true}).then(({data})=>setItems((data as Row[]|null)??[]));},[supabase]);const display:Row[]=items.length?items:fallback.map((d,i)=>({name:d[0],short_description:d[1],slug:String(d[0]).toLowerCase().replace(/[^a-z0-9]+/g,"-"),id:i}));return <main><ManagedHero pageKey="destinations" defaults={{eyebrow:"EXPLORE CHINA",title:"Destinations",subtitle:"From ancient capitals to dramatic mountains, discover a China that matches your travel style.",image:"https://images.unsplash.com/photo-1508804185872-d7badad00f7d?auto=format&fit=crop&w=2200&q=86",overlay:48}}/><section className="destinationIndex"><div className="contentIndexIntro"><div><p className="v1Eyebrow red">FIND YOUR CHINA</p><h2>Places worth traveling for.</h2></div><p>Explore China by city, landscape and travel style. Each destination can be combined into a private journey built around your dates.</p></div><div className="destList">{display.map((d,i)=><article key={String(d.id??i)}><span className="destNumber">{String(i+1).padStart(2,"0")}</span><div className="destCopy"><h2><Link href={"/destination?slug="+encodeURIComponent(String(d.slug??""))}>{String(d.name??"China")}</Link></h2><p>{String(d.short_description??d.description??"Discover this remarkable part of China.")}</p></div><Link className="destExplore" href={"/destination?slug="+encodeURIComponent(String(d.slug??""))}>Explore <span>→</span></Link></article>)}</div></section></main>}